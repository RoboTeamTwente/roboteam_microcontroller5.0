#include "powerMonitor.h"

void PM_I2C(){
    /* To communicate with the INA234, the controller must first address targets through a target address byte. The
    target address byte consists of seven address bits and a direction bit that indicates whether the action is to be a
    read or write operation. */
}